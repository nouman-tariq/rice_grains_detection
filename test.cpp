#include <iostream>

using namespace std;

int main()
{
    FILE * pf;
    pf = fopen("beans_text1.txt", "wb");
    cout<<"End"<<endl;

    if (pf!=NULL)
    {
        fwrite(pf, sizeof(char), )
    }
  
}